module bubble {

  array [1D] Bubble {
    entry Bubble();
    entry void startPhase(int);
    entry void requestSwap(int); 
    entry void acceptSwap(int neighborValue);
    entry void denySwap();
    entry void displayValue(int prefixLen, char prefix[prefixLen]);
  };

};