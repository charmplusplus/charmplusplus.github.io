mainmodule main {

  readonly CProxy_Main mainProxy;
  readonly int numElements;

  extern module bubble;

  mainchare Main {
    entry Main(CkArgMsg* msg);
    entry void phaseCheckin(int);
    entry void arrayDisplayFinished();
  };

  initproc void processorStartupFunc();

};