module prefix {

  array [1D] Prefix {
    entry Prefix();
    entry void displayValue(char prefix[len], int len);
    entry void doStep(int stepNum);
    entry void passValue(unsigned int incomingValue);
  };

};