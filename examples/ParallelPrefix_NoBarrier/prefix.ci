module prefix {

  array [1D] Prefix {
    entry Prefix();
    entry void displayValue(char prefix[len], int len);
    entry void startPrefixCalculation();
    entry void passValue(unsigned int incomingValue, int stepNum);
  };

};